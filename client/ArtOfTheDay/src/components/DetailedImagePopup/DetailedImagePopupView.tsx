import {View} from "react-native";
import DetailedImagePopupViewData from "@/src/components/DetailedImagePopup/DetailedImagePopupViewData";

export default function DetailedImagePopupView({item}: { item: DetailedImagePopupViewData }) {
    return (
        <View>

        </View>
    );
}